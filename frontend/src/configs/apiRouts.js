const API_V1 = 'api/v1';

// eslint-disable-next-line import/prefer-default-export
export const API_ROUTES = {
  CHANNELS: `${API_V1}/channels`,
  MESSAGES: `${API_V1}/messages`,
  LOGIN: `${API_V1}/login`,
  SIGNUP: `${API_V1}/signup`,
};
